import { useState } from "react";
import Head from "next/head";
import { AnimatePresence } from "framer-motion";
import { Header, Footer, PageIntro } from "../../components/SiteChrome";
import { projectsData, ProjectCard, ProjectModal } from "../../components/ProjectDetails";
export default function Projects(){const [selected,setSelected]=useState(null);return <div className="site"><Head><title>PCB & Embedded Hardware Projects — Dhara Rajpura</title><meta name="description" content="Seven PCB and embedded hardware case studies spanning wireless products, robotics, mixed-signal electronics and safety-critical layout."/></Head><Header/><main><PageIntro eyebrow="Project index / 07 systems" title="Hardware case studies, presented with scope intact." copy="Explore engineering decisions, role boundaries, technical highlights and complete visual galleries across seven embedded hardware projects."/><section className="section section-dark"><div className="shell"><div className="project-index-note"><span className="tech-label text-cyan">Visual note</span><p>Gallery artwork is clearly disclosed as concept presentation material. Project descriptions distinguish full hardware design from client-supplied architecture and layout-only scope.</p></div><div className="projects-grid projects-page-grid">{projectsData.map((p,i)=><ProjectCard key={p.slug} project={p} index={i} onOpen={setSelected}/>)}</div></div></section></main><Footer/><AnimatePresence>{selected&&<ProjectModal project={selected} onClose={()=>setSelected(null)}/>}</AnimatePresence></div>}
